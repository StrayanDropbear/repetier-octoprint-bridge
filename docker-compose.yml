services:
  repetier-octoprint-bridge:
    build: .
    image: repetier-octoprint-bridge:latest
    container_name: repetier-octoprint-bridge
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
